export default CONFIG;
declare namespace CONFIG {
    namespace API {
        const BASE_URL: string;
        const VERSION: string;
        namespace METHODS {
            namespace groups {
                namespace queries {
                    const getAll: string;
                    const get: string;
                    const suffix: string;
                }
                const methods: {};
            }
        }
    }
}
